[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "trace_parser"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "trace_parser._native"
python-source = "python"
